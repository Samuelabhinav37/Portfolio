/* Kai chat drawer knowledge base for the about page — see luna-kb-base.js
   for the shared entries. No overrides needed here; the 'about' entry's
   default reply/target already fit this page. */
window.SITE.initLunaDrawer(window.SITE.buildLunaKB());
